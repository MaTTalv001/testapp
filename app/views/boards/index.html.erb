<div class="row">
    <div class="col-12">
      <div class="row">
        <% if @boards.present? %>
        <%= render partial: "board", collection: @boards %> <% else %>
        <p><%= t('.no_result') %></p>
        <% end %>
      </div>
    </div>
  </div>
</div>