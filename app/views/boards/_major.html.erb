<%= link_to board_majors_path(board), 
                  data: { turbo_method: :post},
                  class: "btn btn-outline-primary btn-sm",
                  id: "major-button-for-board-#{board.id}" do %>
<%= "#{board.majors_count}めちゃR" %>
<% end %>